/*
* A calculator function class that handles clear function.
* The class resets the calculator screen output and state.
*
* */
export default class ClearFunction {

    newState = {
        firstNumber: null,
        secondNumber: null,
        operator: null,
        currentScreenExpression: ''
    };

    /*
    * Executing the function
    *
    * */
    execute() {
    }

}